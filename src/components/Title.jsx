import React from 'react';

const titleStyle = {
  textAlign: 'center',
};

const Title = (props) => {
  return (
    <div style={titleStyle} >
      <h1>THIS IS TITLE</h1>
    </div>
  );
};

export default Title;